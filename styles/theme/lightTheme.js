import { createTheme } from "@mui/material/styles";

const lightTheme = createTheme({
  palette: {
    WHITE: "#ffffff",
    primary: {
      main: "#00beff",
    },
  },
});

export default lightTheme;
